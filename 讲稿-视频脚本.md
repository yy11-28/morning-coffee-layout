# 《布局实战：网页排版与视觉细节》作业演示视频讲稿

> 预计时长 12 分钟左右。`【口播】`是要说的话，`▶ 操作`是同时要在页面上做的动作。
> 录制前先把页面 Ctrl+F5 强刷一次、浏览器缩放 100%、从顶部开始录。

---

## 0. 开场 + 项目展板（0:00 – 1:00）

▶ 操作：打开页面，停留在顶部「课程项目说明」展板，鼠标顺着九章卡片扫一遍，再指一下右下角进度条。

【口播】
老师好，我的作业主题是"如何做好页面布局"。整个网站不是语法手册，而是用"从零搭建一家咖啡店的落地页"这一个真实案例，把布局方法串成完整的工作流：九个小节，每节只完成咖啡店页面的一个模块，九节学完，页面就拼装成型。每一节都固定四件套——讲解、关键代码、可交互演示、自学自测。顶部的项目展板就是这个体系的入口：下面这排卡片是九大章节的技术链路，点任意一卡可以直接跳到对应章节；右下角是我的实时学习进度，它由我在这门课里的真实学习行为自动统计，后面我会专门讲。

---

## 1. 第1节 布局核心概念——先想清楚，再动手（1:00 – 2:00）

▶ 操作：侧边栏点「01」，切到「演示 Demo」标签。

【口播】
第一节先立规矩：布局不是把元素摆上去，而是信息设计。写任何 CSS 之前先回答三个问题——内容怎么分组、谁重要谁次要、什么东西对齐成一条线。这节的可交互演示把三个原则做成了开关。

▶ 操作：依次勾选「结构分组」「层级表达」「对齐统一」三个开关，再把「留白控制」滑杆拖大拖小，最后点「灰度测试」，停两秒。

【口播】
大家可以注意「灰度测试」这个按钮——它把所有颜色去掉只看黑白骨架。这是我做布局的习惯：先看骨架对不对，再穿衣服。结构对了，皮肤才有意义。

---

## 2. 第2节 CSS 盒模型——布局的"占位计算"（2:00 – 2:50）

▶ 操作：切到「02 CSS 盒模型」的 Demo。

【口播】
布局错位、间距算不对，十有八九是盒模型没算清。核心理论就一句话：盒子的总宽 = 内容 + 内边距 + 边框 + 外边距，而 box-sizing 决定了 width 到底"算到哪"。这不是背语法，是工程决策——咖啡店的菜单卡片要做多列网格，如果用默认的 content-box，加了内边距卡片就会撑破行列；统一改成 border-box，写多大就是多大，网格永远整齐。

▶ 操作：依次点「content-box」「border-box」「margin-compare」三个按钮，每个停一秒，指一下宽度标注的变化；hover 卡片看区域高亮。

【口播】
切换按钮可以实时看到同一个 width 在两种模式下的视觉宽度差异。第三种模式还演示了 margin 折叠——垂直方向相邻外边距会合并，这是很多同学页面间距"时大时小"的元凶。

---

## 3. 第3节 文本流布局——一切布局的基线（2:50 – 3:40）

▶ 操作：切到「03 文本流布局」Demo。

【口播】
在学 Flex 和 Grid 之前必须理解浏览器默认的排版规则：文本流。块级元素独占一行，行内元素随文字流动，行内块可以设宽高。默认规则都不理解，定位和弹性布局出了问题就无从排查。

▶ 操作：「元素类型」下拉依次切 div / span / img；拖「宽度」和「上下 margin」滑杆；最后勾上「浮动」开关，指着父容器边框高亮。

【口播】
切换元素类型，能直观看到三种元素在文档流里的占位差异。开启浮动后注意两个现象：文字环绕图片——浮动的设计初衷；以及父容器高度塌陷——边框高亮就是塌陷信号，这也是后面clearfix和 BFC 的由来。

---

## 4. 第4节 Position 定位——规则之外的"例外"（3:40 – 4:30）

▶ 操作：切到「04 Position 定位」Demo（这节有两个实验台，先讲网格那个一嘴带过，重点玩定位台）。

【口播】
定位的本质是让元素脱离或偏移正常文档流，它是"例外规则"，要节制使用。这节演示台可以实时调四个变量：定位模式、top、left、z-index，还有一个很关键的开关——父级是否 relative。

▶ 操作：定位模式切到 absolute；拖 top / left 滑杆；把 z-index 从 0 拖到 10 看压盖变化；最后取消勾选「父级开启 relative」，看方块瞬间飞到视口边缘，再勾回来。

【口播】
重点看刚才那一下：absolute 的参照物是"最近的非 static 祖先"，父级一关 relative，参照物变成视口，元素直接飞出去——这一下就能记住 absolute 的定位原理。z-index 则说明层级是有上下文的，不是全局比大小。

---

## 5. 第5节 Flex 布局——一维排布的利器（4:30 – 5:20）

▶ 操作：切到「05 Flex 布局」Demo。

【口播】
Flex 解决一维布局：一条主轴上怎么排。咖啡店页面里，导航栏、按钮组、三张特色卡，都是典型的一维场景，Flex 是首选。它的思维模型是"主轴 + 侧轴"两个方向的控制。

▶ 操作：主轴方向切 column 再切回 row；justify-content 依次切 space-between / center / flex-end；把 flex-wrap 开关关掉，拖窄容器宽度指挤压变形；点「重置」收尾。

【口播】
注意关闭换行那一幕：空间不足时元素被硬挤压变形——这就是为什么实际项目几乎总是写 flex-wrap: wrap，让装不下的元素自动下沉，而不是被挤坏。

---

## 6. 第6节 Grid 布局——二维网格与选型思维（5:20 – 6:10）

▶ 操作：切到「06 Grid 布局」Demo。

【口播】
Grid 是二维布局：先划好行列轨道，再把元素放进格子。咖啡菜单六款咖啡要排成整齐的多行多列、主推款还要占两格做大卡——这就是 Grid 的标准场景。fr 单位按比例分配剩余空间，天然适合响应式。

▶ 操作：gridlab 模式下拉依次切「均等网格」「比例网格」「主推突出」；拖 gap 滑杆；指一下主推大卡跨两列的区域。

【口播】
这里要说布局技术选型的判断标准：只有一条轴要排，用 Flex；行和列都要管，用 Grid。我的页面里导航用 Flex、菜单用 Grid，就是按这个决策来的，而不是哪个新用哪个。

---

## 7. 第7节 响应式布局——移动端优先（6:10 – 7:10）

▶ 操作：切到「07 响应式布局」Demo。

【口播】
响应式的核心理论是"移动端优先"：默认样式先写手机版，再用 min-width 媒体查询逐级向上增强——平板两列、桌面四列。反过来从桌面往下砍叫"桌面降级"，很容易漏掉手机档，小屏被硬挤。这决定了我写媒体查询时用的是 min-width 而不是 max-width。

▶ 操作：设备下拉切 手机 → 平板 → 桌面；再把「容器宽度」滑杆慢慢拖窄，看列数在断点处自动降级；提一句代码里 rem / vw 的用法。

【口播】
看宽度滑杆连续拖动的过程：列数不是连续变的，而是在断点处跳变——这就是断点的意义，每一档都保证有可用的布局。间距字号我用 rem 保持比例，横幅用 vw 占视口宽度。

---

## 8. 第8节 视觉细节——8px 体系收尾（7:10 – 8:00）

▶ 操作：切到「08 视觉细节」Demo。

【口播】
视觉规范是最后一步，而且必须服务内容。我的约束是：间距、圆角、阴影只允许取 8px 的倍数——这套"设计令牌"让整个页面自动获得统一感；字号只保留三层层级；所有按钮必须有 hover 和按压反馈。

▶ 操作：风格下拉切「聚焦版」「高级版」；拖圆角滑杆从 0 到 20；开合「阴影」开关；hover 一下点单按钮。

【口播】
拖圆角的过程里，卡片从生硬变得友好——但关键是所有卡片一起变，这就是令牌化的价值：改一处，全局一致。

---

## 9. 第9节 综合实战——四阶段装配台（重点，8:00 – 9:30）

▶ 操作：切到「09 综合实战案例」的 Demo，页面应该已经是四阶段装配台。先点「① 结构骨架」，停两秒。

【口播】
最后一节把前面八节串成完整的工程流程，也是我认为这个作业最核心的原创点——四阶段装配台。它把"先结构、再布局、再适配、最后视觉"这个真实的工作流，做成了可以分步播放的演示。

▶ 操作：点「② 布局排版」，指导航变横排、三卡等高、菜单三列。

【口播】
阶段一，纯结构：只有语义化 HTML，不写一行 CSS，所有区块按文档流从上往下堆，先确认内容顺序对不对——这就是第一节"结构优先"原则的落地。阶段二，布局上场：Flex 让导航横排、Grid 把菜单排成三列、定位把购物车角标钉在右上角，此时故意不上一滴颜色，让大家只关注"位置对不对"。

▶ 操作：点「③ 响应式适配」，此时右侧手机/平板/桌面按钮解锁，点「手机」，预览框变窄、菜单降为单列；再点「桌面」。

【口播】
阶段三，适配：同一套 HTML，按断点在手机、平板、桌面之间切换列数，这就是第七节移动端优先原则的实战。注意左侧设备按钮在阶段三才解锁——因为适配本来就是第三步。

▶ 操作：点「④ 视觉成品」，页面从灰模变成蓝色品牌成品；然后点「▶ 自动装配」，让它自动从①走到④，期间不要说话，让它播完。

【口播】
阶段四，成品：统一色彩、圆角、阴影、8px 间距和 hover 反馈，页面从"排好了"变成"好看好用"。自动装配按钮可以把整个生长过程一键播放——一个页面是怎么从一堆方块长成成品的，一目了然。

---

## 10. 核心创新点详解（重点，9:30 – 12:00）

▶ 操作：回到页面顶部。

【口播】
除了九节内容本身，我为这个作业做了一套完整的学习产品化设计，逐个介绍。

**创新一：全站中英双语，内容 100% 一致。**
▶ 操作：点右上角语言按钮切到 EN，扫过展板和正文；进任意一节「关键代码」，指代码块里的英文注释；再切回中文。

【口播】
全站一千两百多条文案全部双语映射，讲解、自测、弹窗提示、按钮状态全部同步切换；最关键的是代码块里的中文注释也做了逐条英译字典，代码结构、变量说明、注释完全统一，不存在"翻译了一半"的页面。切回中文。

**创新二：学习轨迹系统——用行为数据证明学习过程。**
▶ 操作：点左下角 📈 悬浮球，展开面板，指九节的三色圆点；拖动页面滚两屏再收起。

【口播】
每一节有三个状态：阅读、实验、自测。阅读靠 IntersectionObserver 检测——某节滚动进入视口超过两成自动记为已读，不需要点击；实验必须真实操作过 Demo 控件才算，完成后面板上会弹出绿色的"已完成实验"角标；自测记录提交次数、历史最高分和错题号。全部数据存在浏览器 localStorage 里，刷新不丢。悬浮球展开就是九节进度明细和总完成度，老师一眼能看到学习闭环的证据。右下角也可以一键重置。

**创新三：知识点「对错案例」总览。**
▶ 操作：点展板上的「查看对错案例总览」，弹窗出现后依次指四组对比；在第三组点「二维矩阵（Grid）」切一下；点「关闭」。

【口播】
这个弹窗把整门课最容易犯的错误集中成正误对照：固定 px 导致溢出对比流式单位、移动端优先对比桌面降级、Flex 一维对比 Grid 二维、规范 UI 对比杂乱 UI。每组都配了可以直接跳回对应章节复习的按钮，等于把全课程的重难点浓缩成一页"避坑指南"。

**创新四：自测错题复盘。**
▶ 操作：随便进一节自测，故意提交一个错误答案，指着下方生成的错题卡；点「回到讲解复习 ↩」，页面跳回讲解。

【口播】
自测提交后不止给分：每道错题自动生成复盘卡，附错误原因解析和知识点定位，一键跳回对应讲解重新学习，错题还会同步汇总到悬浮球里。让自测从"打个分"变成"补漏洞"。

**创新五：一键导出学习报告。**
▶ 操作：点悬浮球面板里的「导出学习报告」（或展板上的按钮），等浏览器下载提示出现。

【口播】
一键导出一个独立 HTML 学习报告：总体完成度、自测正确率、九节的阅读实验自测明细、错题和解析全都在里面，可以直接作为结课报告附件交给老师。这份报告不是手填的，全部来自前面说的行为追踪数据。

**创新六：工程级细节统一。**
▶ 操作：点主题按钮切暗色再切回来，展示展板/弹窗/悬浮球在暗色下的效果。

【口播】
全站颜色全部由 CSS 变量驱动，亮暗主题一键切换，所有新增组件自动适配；所有控件有统一的过渡时长、hover 反馈和键盘焦点样式。这本身就是第8节"8px 体系、统一令牌"理论在我自己代码里的实践。

---

## 11. 收尾（12:00 – 12:30）

▶ 操作：回到顶部展板，画面停在进度条上收尾。

【口播】
总结一下这个作业：一个"晨光咖啡"案例贯穿九节，从布局原则、盒模型、文档流、定位，到 Flex、Grid、响应式、视觉规范，最后用四阶段装配台完成综合实战；每节都是讲解、代码、可交互演示、自测的完整闭环；再加上双语、学习轨迹、错题复盘和可导出的学习报告。它不只是"讲布局"的网页，而是一个能证明"我真的这样学了、这样做了"的完整学习产品。谢谢老师。

---

## 附：录制前检查清单

1. Ctrl+F5 强制刷新，确认左下角有 📈 悬浮球、顶部有展板（说明加载的是最新版）。
2. 先随便滚动 + 操作一两节 Demo + 提交一次自测，让进度条和角标有数据，录像里更好看（或者反之，从头演示增长过程）。
3. 浏览器缩放 100%，窗口宽度 ≥1200px，保证四阶段预览完整。
4. 第9节「自动装配」播放一轮约 3 秒，提前试一次找好暂停话术。
5. 导出报告会真实下载一个 HTML 文件，录之前清一下下载栏，避免遮挡。
6. 英文模式只需展示 20 秒左右（展板 + 一段代码注释），讲完立刻切回中文，避免后续口播与界面语言对不上。
    