# 网页功能说明文档

> **项目名称**：「晨光咖啡」布局实战教学站  
> **课程**：第六章 布局实战：网页排版与视觉细节  
> **技术栈**：纯原生 HTML / CSS / JavaScript，零依赖、零框架  
> **文件**：index.html · style.css · script.js  

---

## 一、项目概述

本项目是一个**交互式 CSS 布局教学网站**，以「晨光咖啡」咖啡店落地页为贯穿案例，将九个布局知识点串联成一条完整的工程链路。网站不只是静态教程，而是集**讲解、关键代码、可交互演示、自测评分、错题复盘、学习轨迹、学习报告导出**于一体的教学产品。

**核心理念**：布局不是"把元素摆好"，而是"让信息有顺序、有层级、有呼吸"。每节先讲"为什么这样放"的布局理论，再给最小代码证据，最后用动态演示验证。

---

## 二、九大章节内容概览

每节均含「📘 讲解 / 💻 关键代码 / 🎬 演示 Demo / 自学自测」四个标签页。

| 小节 | 主题 | 核心理论 | 演示交互 |
|---|---|---|---|
| 01 | 布局核心概念 | 结构优先、对齐统一、留白控制、层级表达 | 整页蓝图预览 |
| 02 | CSS 盒模型 | box-sizing 两种模型、margin 折叠 | content-box/border-box 切换 + margin 对比 |
| 03 | 文本流布局 | 块级/行内/inline-block 占位差异、浮动与塌陷 | display 切换 + 浮动父容器塌陷演示 |
| 04 | Position 定位 | absolute 参照物、fixed 吸顶、z-index 上下文 | 父级 relative 开关 + 定位模式切换 |
| 05 | Flex 布局 | 主轴/侧轴思维、justify/align、wrap 必要性 | 列数切换 + gap 滑杆 + wrap 开关 |
| 06 | Grid 布局 | fr 比例分配、跨列做视觉重点、Flex/Grid 选型 | 列数下拉 + gap 滑杆 + 突出主模块开关 |
| 07 | 响应式布局 | 移动端优先 vs 桌面降级、断点、rem/vw | 设备宽度切换 + 断点跳变 |
| 08 | 视觉细节 | 8px 间距体系、三层字级、圆角阴影、hover 反馈 | 规范/杂乱 UI 实时对比 |
| 09 | 综合实战 | 四阶段工作流：结构→布局→适配→视觉 | 四阶段分步装配台（详见下文） |

---

## 三、关键亮点

### 亮点 1：单一案例贯穿九节，工程逻辑完整

全站以「晨光咖啡」咖啡店落地页为唯一案例。第 1 节拆解页面模块（导航→横幅→特色卡→菜单九宫格→购物车角标→页脚），此后每节只完成其中一个模块，第 9 节拼装成完整页面。老师可以清晰看到从"一段需求文字"到"完整上线页面"的工程闭环。

### 亮点 2：每节演示均为可交互动态 Demo

九节 Demo 全部支持实时操作——切按钮看效果、拖滑杆调间距、开关 wrap/relative 对比正误。不是静态截图或播放动画，而是真实可操作、即时反馈的布局实验台。

**典型示例**：
- 第 4 节：关掉"父级 relative"开关，absolute 方块立即"飞出"父容器，直观证明参照物规则
- 第 5 节：关掉 wrap，卡片被压缩变形；打开后自动换行——一张图讲清为什么"wrap 几乎必写"
- 第 6 节：拖 gap 滑杆同时切列数，网格间距和轨道数实时联动

### 亮点 3：第 9 节四阶段分步装配台（核心原创）

这是全站最大的创新点。第 9 节的 Demo 不是简单展示成品页面，而是一个**可分步播放的装配台**，模拟专业前端真实工作流：

| 阶段 | 预览窗显示 | 理论依据 |
|---|---|---|
| ① 结构骨架 | 全部区块退化为灰色线框、纵向堆叠，无 CSS | 结构优先：先确认内容顺序 |
| ② 布局排版 | Flex 导航横排、Grid 菜单三列、定位角标，但不上色 | 布局是"排位置"，与视觉分离 |
| ③ 响应式适配 | 解锁手机/平板/桌面设备按钮，切换预览宽度 | 同一套 HTML 适配多设备 |
| ④ 视觉成品 | 品牌配色、圆角阴影、hover 反馈全部回归 | 视觉是最后一步，服务内容 |

- 类名是**累积的**（点③时页面同时挂 `fs-1 fs-2 fs-3`），每一步建立在上一步之上
- 附「▶ 自动装配」按钮，每 950ms 自动走一阶段，页面当众从方块长成成品
- 设备按钮在阶段③才解锁——因为适配本来就是第三步

### 亮点 4：100% 中英双语，内容绝对一致

网站支持中文/英文一键切换，覆盖范围包括：
- 页面所有文本（标题、正文、侧边栏、标签页）
- 所有动态提示文案（演示说明、按钮文字、角标）
- 自测题目和选项
- 错题解析
- **代码注释**（55 条唯一注释，英文模式下自动替换为英文，含 MutationObserver 覆盖动态代码块）
- 导出的学习报告 HTML

双语切换零延迟、零遗漏，代码注释的中英字典独立于顶部 I18N 系统，通过 `window.__CODE_COMMENTS_EN__` 管理。

### 亮点 5：学习轨迹系统（教学闭环证据）

网站通过三种检测机制自动记录学习行为，存储在浏览器 localStorage（键 `layout-learning-v1`）：

| 检测维度 | 机制 | 判定标准 |
|---|---|---|
| 📖 阅读 | IntersectionObserver（threshold 0.2） | 某节内容滚动到视口 20% 即自动标记已读 |
| 🧪 实验 | 监听 Demo 面板内 input/change/click 事件 | 在演示区发生任意一次真实操作即标记完成 |
| ✅ 自测 | 监听自测提交按钮 | 提交一次即标记完成，记录最高分和错题题号 |

- 总进度 = 9 节 × 3 维度 = 27 个完成点，按比例显示百分比
- 顶部展板进度条和左下角悬浮球实时同步
- 实验/自测完成后演示区弹出绿色「✓ 已完成」角标
- 悬浮球内可查看九节明细、点击错题直达对应题目、一键重置

### 亮点 6：知识点对错案例总览弹窗

从顶部展板或悬浮球可打开一个对比总览弹窗，整合全课程四个核心对比：

1. **px 固定列 vs fr 流式列**：固定宽度溢出 vs 按比例伸缩
2. **移动端优先 vs 桌面降级**：从单列向上增强 vs 从四列向下删减
3. **Flex（一维）vs Grid（二维）**：一键切换两种布局模式，实时看差异
4. **规范 UI vs 杂乱 UI**：8px 间距+三层字级+统一圆角 vs 随机边距+直角无阴影

每组对比附「回对应章节复习」跳转按钮，老师可一键看完整本课程核心重难点。

### 亮点 7：自测系统 + 错题复盘

- 每节 4 题（响应式 6 题），支持中英文作答
- 提交后自动评分，显示得分百分比和反馈语
- **错题复盘**：提交后在自测面板自动生成错题卡片，含题号、双语错误原因解析、「回到讲解复习」跳转按钮
- 错题列表同步进悬浮球，点击直达对应题目
- 错题随重测自动更新，取历史最高分

### 亮点 8：一键导出学习报告

悬浮球内点击「导出报告」按钮，一键下载独立 HTML 文件，内容包括：
- 总体完成度、自测正确率（三大 KPI 卡片）
- 九节学习进度表格（阅读/实验/自测状态 + 各节成绩）
- 错题汇总（含双语解析）
- 文件名随语言切换：`晨光咖啡-学习报告.html` / `morning-coffee-learning-report.html`

导出的报告文件可独立打开，适合作为结课作业报告附件提交。

### 亮点 9：顶部作业说明展板

页面顶部自动生成项目展板，内容包括：
- 课程项目介绍和案例说明
- 「讲解/代码/演示/自测」四件套标签
- 九大章节技术链卡片（点击直达各节）
- 实时学习进度条（与悬浮球同步）
- 「对错案例总览」和「导出报告」快捷入口

### 亮点 10：亮暗主题 + 全局细节统一

- 支持**亮色/暗色主题**一键切换，所有新组件全部使用 CSS 变量（`--bg/--card/--text/--muted/--accent` 等），主题切换时色彩体系统一
- 全站按钮统一 0.18s 过渡 + hover/active 按压反馈
- 弹窗、角标、进度条均有淡入动画
- 页头「本章路径」思维导图按钮支持点击跳转，带到达校验兜底（平滑滚动失败时自动补一次瞬时定位）
- 侧边栏滚动高亮当前章节

---

## 四、技术实现要点

### 4.1 文件结构

| 文件 | 行数 | 职责 |
|---|---|---|
| index.html | ~2159 | 页面结构、九节内容、代码块、Demo 容器、自测表单 |
| style.css | ~4000+ | 全站样式、CSS 变量、暗色主题、四阶段覆写、所有新组件 |
| script.js | ~3800+ | I18N 双语系统、Tab 切换、9 套演示交互、9 套评分器、升级 IIFE（装配台/轨迹/展板/弹窗/报告） |

### 4.2 双语系统架构

- **顶部 I18N**：`I18N_EN` 字典（约 1227 条），覆盖 HTML 文本节点和属性，通过 MutationObserver 监听 DOM 变化自动翻译
- **升级 IIFE 字典**：`FE_STRINGS`（约 90 条），专供升级模块的新 UI 文案，通过 `t()` 函数读取
- **代码注释字典**：`window.__CODE_COMMENTS_EN__`（55 条），仅英文模式启用，按"空白折叠归一"的键匹配注释全文并替换
- **自测评分**：9 套评分器均支持中英文关键词判分（如 padding/margin、flex/grid、viewport 等）

### 4.3 学习轨迹存储

```
localStorage 键: layout-learning-v1
结构: {
  v: { "overview": true, "textflow": false, ... },  // 阅读状态
  d: { "overview": true, ... },                      // 实验状态
  q: { "overview": true, ... },                      // 自测状态
  scores: { "overview": "4/4", ... },                // 最高分
  wrong: { "overview": [2], "flex-quiz": [1,3], ... } // 错题题号
}
```

### 4.4 四阶段装配台实现

- 向第 9 节 Demo 的 `.demo-shell` 注入 `.finlab-stagebar`（4 个阶段按钮 + 3 个设备按钮 + 自动装配按钮）
- 点击阶段按钮时，给 `.finlab-page` 添加累积类名 `fs-1 fs-2 fs-3 fs-4`
- CSS 用 `:not(.fs-2)` 等选择器实现阶段覆写（如 `.finlab-page:not(.fs-2) .finlab-nav { display:block }`）
- 设备按钮在 `state.stage < 3` 时 disabled + `.is-locked` 灰显

---

## 五、使用指南

### 5.1 基本浏览
1. 打开 `http://localhost:8000/index.html`
2. 左侧侧边栏点击任意章节跳转
3. 每节内切换「讲解 / 关键代码 / 演示 Demo / 自学自测」标签

### 5.2 交互演示
- 点击 Demo 区的按钮/滑杆/下拉框，实时看布局变化
- 第 9 节：点四个阶段按钮逐层解锁，或点「▶ 自动装配」一键播放
- 第 9 节阶段③：点手机/平板/桌面按钮切换设备预览

### 5.3 自测与复盘
- 切到「自学自测」标签，答题后点提交
- 提交后自动评分 + 生成错题复盘卡
- 错题卡内点「回到讲解复习」跳回对应讲解

### 5.4 学习轨迹
- 正常浏览和操作即可自动记录进度
- 左下角悬浮球显示总完成度百分比
- 点击悬浮球展开九节明细面板
- 错题列表内点击错题直达对应自测题

### 5.5 导出报告
- 点击悬浮球面板内「导出报告」按钮
- 下载独立 HTML 报告文件，可直接提交老师

### 5.6 双语与主题
- 页面顶部右侧有「中/EN」语言切换和「☀/🌙」主题切换
- 切换语言后全站内容（含代码注释）即时翻译
- 切换主题后所有组件色彩统一适配

---

## 六、设计理念总结

| 维度 | 传统教学站 | 本项目 |
|---|---|---|
| 案例关联 | 每节独立示例 | 单一案例贯穿九节 |
| 演示方式 | 静态截图/代码 | 可交互动态 Demo |
| 评测 | 无或仅末尾考试 | 每节自测 + 错题复盘 + 跳转复习 |
| 学习追踪 | 无 | 三态自动检测 + 进度可视化 + 报告导出 |
| 双语 | 无或不完整 | 100% 含代码注释，内容绝对一致 |
| 工作流展示 | 无 | 四阶段装配台分步播放 |
| 对比教学 | 无 | 四组核心对错案例总览弹窗 |

本项目从"零散学属性"升级为"工程闭环教学产品"，具备**理论 + 实操 + 代码 + 自测 + 复盘 + 报告**的完整教学链路。
